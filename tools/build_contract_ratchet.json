{
  "_comment": "Deferred, pre-existing build-contract violations. This ratchet ONLY SHRINKS: when an entry stops violating, the gate FAILS until it is deleted, so a fixed violation can never silently re-permit itself. Do not add entries for new code \u2014 new violations must be fixed, not deferred. Remaining base-image entries are blocked ONLY on Docker Hub's 100/hour anonymous manifest limit, not on any decision: run `python3 tools/preflight_build_contract.py --heal` from a host with quota (or an authenticated `docker login`) and they resolve.",
  "unpinned_base_images": {
    "apps/api/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal api`. The ratchet only shrinks.",
    "apps/embeddings/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal embeddings`. The ratchet only shrinks.",
    "apps/entity-resolution/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal entity-resolution`. The ratchet only shrinks.",
    "apps/eval-fabric-api/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal eval-fabric-api`. The ratchet only shrinks.",
    "apps/evidence-console/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal evidence-console`. The ratchet only shrinks.",
    "apps/evidence-receipts/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal evidence-receipts`. The ratchet only shrinks.",
    "apps/gateway/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal gateway`. The ratchet only shrinks.",
    "apps/gbrg-engine/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal gbrg-engine`. The ratchet only shrinks.",
    "apps/grl-mesh/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal grl-mesh`. The ratchet only shrinks.",
    "apps/grlplus-service/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal grlplus-service`. The ratchet only shrinks.",
    "apps/health-twin/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal health-twin`. The ratchet only shrinks.",
    "apps/hellgraph-service/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal hellgraph-service`. The ratchet only shrinks.",
    "apps/holmes/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal holmes`. The ratchet only shrinks.",
    "apps/identity-twin/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal identity-twin`. The ratchet only shrinks.",
    "apps/ie-engine/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal ie-engine`. The ratchet only shrinks.",
    "apps/lattice-forge/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal lattice-forge`. The ratchet only shrinks.",
    "apps/lattice-studio/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal lattice-studio`. The ratchet only shrinks.",
    "apps/liberty-stack-readout/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal liberty-stack-readout`. The ratchet only shrinks.",
    "apps/lifecycle-warden/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal lifecycle-warden`. The ratchet only shrinks.",
    "apps/market-replay/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal market-replay`. The ratchet only shrinks.",
    "apps/memoryd/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal memoryd`. The ratchet only shrinks.",
    "apps/node-commander/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal node-commander`. The ratchet only shrinks.",
    "apps/nugget-extractor/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal nugget-extractor`. The ratchet only shrinks.",
    "apps/osm-map-api/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal osm-map-api`. The ratchet only shrinks.",
    "apps/portfolio-agent/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal portfolio-agent`. The ratchet only shrinks.",
    "apps/prophet-materializer-clickhouse/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal prophet-materializer-clickhouse`. The ratchet only shrinks.",
    "apps/reasoning-failure-runner/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal reasoning-failure-runner`. The ratchet only shrinks.",
    "apps/receipt-gateway/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal receipt-gateway`. The ratchet only shrinks.",
    "apps/regis-acr-api/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal regis-acr-api`. The ratchet only shrinks.",
    "apps/reporting-watcher/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal reporting-watcher`. The ratchet only shrinks.",
    "apps/search-gateway/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal search-gateway`. The ratchet only shrinks.",
    "apps/sherlock-engine/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal sherlock-engine`. The ratchet only shrinks.",
    "apps/socioprophet-web/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal socioprophet-web`. The ratchet only shrinks.",
    "apps/sophos-reasoner/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal sophos-reasoner`. The ratchet only shrinks.",
    "apps/spark-runner/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal spark-runner`. The ratchet only shrinks.",
    "apps/synapse-bridge/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal synapse-bridge`. The ratchet only shrinks.",
    "apps/wordops-capability-broker/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal wordops-capability-broker`. The ratchet only shrinks.",
    "apps/wordops-mcp-gateway/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal wordops-mcp-gateway`. The ratchet only shrinks.",
    "apps/wordops-room-factory/Dockerfile": "Pre-existing at the 2026-08-04 supply-chain audit. Moving base tag: the build is not reproducible. Heal with `python3 tools/preflight_build_contract.py --heal wordops-room-factory`. The ratchet only shrinks."
  },
  "unpinned_requirements": {}
}
