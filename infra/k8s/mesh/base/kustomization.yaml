apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Mesh policy + Gateway. Requires Cilium (CNI + gatewayAPI) and the Gateway API
# CRDs — installed by the mesh Argo app at an earlier sync-wave.
resources:
  - cilium-networkpolicy.yaml
  - gateway.yaml
