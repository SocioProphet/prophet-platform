apiVersion: v1
kind: ServiceAccount
metadata:
  name: hellgraph-edge-twin-sync
  namespace: socioprophet
  labels:
    bundle: edge-twin-sync
  # On GKE, annotate with the workload-identity SA that can write the twin bucket:
  #   iam.gke.io/gcp-service-account: edge-twin-sync@<project>.iam.gserviceaccount.com
