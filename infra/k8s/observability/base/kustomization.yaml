apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - otel-collector-configmap.yaml
  - otel-collector-deployment.yaml
  - otel-collector-service.yaml
  - servicemonitor.yaml
  - prometheusrule-slos.yaml
  # capture + self-verifying liveness (added: Loki was ingesting 0 for 2.5d)
  - promtail.yaml
  - telemetry-canary.yaml
  - telemetry-servicemonitors.yaml
  - telemetry-liveness.yaml
