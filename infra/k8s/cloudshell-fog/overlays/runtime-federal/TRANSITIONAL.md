# Transitional overlay

This overlay is retained for continuity only.

Do **not** treat this as the preferred deployment lane for cloudshell-fog.

Preferred runtime lanes:

- `infra/k8s/cloudshell-fog/overlays/runtime-v2-standard`
- `infra/k8s/cloudshell-fog/overlays/runtime-v2-federal`

Reason:

The v2 overlays explicitly repair the earlier runtime wiring and align with the current policy/inventory contract set.
