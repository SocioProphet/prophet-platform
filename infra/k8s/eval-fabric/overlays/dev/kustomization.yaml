apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prophet-platform-dev
resources:
  - ../../base
patches:
  - target:
      kind: Deployment
      name: eval-fabric-api
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
