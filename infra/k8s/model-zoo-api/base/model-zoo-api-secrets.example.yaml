# Manual bootstrap secret — use this only if External Secrets Operator is not installed.
# After applying: kubectl apply -f this-file -n socioprophet
# Then delete the local copy — never commit with real values.
apiVersion: v1
kind: Secret
metadata:
  name: model-zoo-api-credentials
  namespace: socioprophet
type: Opaque
stringData:
  promotion-gate: REPLACE_WITH_PROMOTION_GATE_TOKEN
