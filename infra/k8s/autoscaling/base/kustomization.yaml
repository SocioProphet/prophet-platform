apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The vLLM scale-to-zero ScaledObject deploys (targets the real serving
# Deployment). The HPA and ScaledJob are adoption examples (target-specific) and
# are not included here.
resources:
  - keda-scaledobject-vllm.yaml
