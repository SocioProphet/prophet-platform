# Transitional Argo application

This Argo application path is retained for continuity only.

Do **not** treat `cloudshell-fog-runtime-federal-application.yaml` as the preferred deployment entrypoint.

Preferred runtime application:

- `cloudshell-fog-runtime-v2-federal-application.yaml`

Preferred federal stack entrypoint:

- `cloudshell-fog-stack-federal-application.yaml`

Reason:

The v2 runtime federal lane aligns with the repaired overlay model, the deployment inventory contract, the compliance profile, and the go-live gate.
