# Transitional Argo application

This Argo application path is retained for continuity only.

Do **not** treat `cloudshell-fog-runtime-application.yaml` as the preferred deployment entrypoint.

Preferred runtime application:

- `cloudshell-fog-runtime-v2-standard-application.yaml`

Preferred standard stack entrypoint:

- `cloudshell-fog-stack-standard-application.yaml`

Reason:

The v2 runtime lane aligns with the repaired overlay model, the deployment inventory contract, and the go-live gate.
