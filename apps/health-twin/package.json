{
  "name": "health-twin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Digital Health Twin engine (walking skeleton) \u2014 opt-in, local-first, sovereign FHIR-lite record store + governed consent grants. Synthetic data only; non-diagnostic.",
  "dependencies": {
    "tsx": "^4.19.2"
  },
  "scripts": {
    "start": "tsx src/server.ts",
    "test:invariants": "tsx src/invariants.ts",
    "test:eval": "tsx src/eval.ts",
    "smoke": "tsx src/smoke.ts",
    "eval:triage": "tsx src/triage-eval.ts",
    "fhir:prove": "tsx src/fhir-live.ts",
    "test": "node --import tsx --test src/*.test.ts"
  }
}
