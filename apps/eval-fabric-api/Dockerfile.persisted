FROM python:3.12-slim
WORKDIR /app
COPY requirements-persisted.txt ./requirements-persisted.txt
RUN pip install --no-cache-dir -r requirements-persisted.txt
COPY app ./app
EXPOSE 8080
CMD ["uvicorn", "app.persisted_main:app", "--host", "0.0.0.0", "--port", "8080"]
