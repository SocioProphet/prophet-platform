[project]
name = "grlplus-service"
version = "0.1.0"
description = "GRLPlus graph-integrated policy evaluator (the symbolic shield over HellGraph)"
requires-python = ">=3.11"
dependencies = ["fastapi==0.115.0", "uvicorn==0.30.6", "httpx==0.27.2"]

[tool.setuptools.packages.find]
where = ["src"]
