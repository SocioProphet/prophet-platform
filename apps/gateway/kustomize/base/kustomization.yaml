apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: gateway-config
    literals:
      - TRITRPC_TARGET_ADDR=tcp://socioprophet-api:9000
      - GATEWAY_PORT=8080
      - TRITRPC_ALLOW_INSECURE_DEV_KEY=0
